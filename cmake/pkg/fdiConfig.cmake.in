get_filename_component(_dir "${CMAKE_CURRENT_LIST_FILE}" PATH)
get_filename_component(_prefix "${_dir}/../../.." ABSOLUTE)

include("${_prefix}/share/cmake/targets/@PROJECT_NAME@-targets.cmake")

# exported variables
set(FIELD-DECOMPOSITION-ISLAND_MODE "@FIELD-DECOMPOSITION-ISLAND_MODE@")


message(STATUS "@PROJECT_NAME@: ${_prefix}")
